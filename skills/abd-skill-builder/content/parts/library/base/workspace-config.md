# Workspace config (pointer)

**Normative content** for **`skill_path`**, **`skill_workspace`**, **`skill-config.json`**, keys, and **`set_workspace.py`** lives in the **phase** doc — not here:

**[`../phases/workspace-and-config.md`](../phases/workspace-and-config.md)** (Phase 0 — **Workspace and config**).

**Greenfield scaffold:** **`templates/skill-scaffold/`** carries **`skill-config.json`** at the skill root (no separate **`conf/`** folder in **abd-skill-builder**).

This file is **not** listed in **`skill-config.json` → `library_files`** (avoid duplicating the phase body in **`AGENTS.md`**). Link the **phase** for new work; this stub remains for older cross-references to **`workspace-config.md`**.
