# rules/

Add atomic rule files here when this skill enforces **must-holds**. Register scanners in **`rules/scanners.json`** and list scanner scripts under **`skill-config.json`** → **`operator.scanners`**.

This example ships **`scanner_smoke.py`** only.
