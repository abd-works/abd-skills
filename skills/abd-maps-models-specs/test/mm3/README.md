# MM3 workspace (`test/mm3`)

**Active skill workspace** — referenced by **`conf/abd-config.json`**:

```json
"active_skill_workspace": "test/mm3"
```

Path is **relative to the `abd-maps-models-specs` skill root** (`skills/abd-maps-models-specs/`).

This directory holds the **live** `solution.conf`, inputs, and generated outputs under **`abd-maps-models-specs/`** when you run the skill. For a **bundled MM3 fixture** (snapshots, tests), see **`../fixture/mm3/`** and **[`../README.md`](../README.md)**.
