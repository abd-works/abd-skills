# MM3 — maps / models / specs (reset)

This folder was cleared to restart **before** `map-model-spec.json` work.

**Kept (sibling `context/`):**

- `../context/chunks/*.md` — evidence units
- `../context/context_index.json` — section manifest / chunk linkage

**Removed:** prior `map-model-spec.json`, `map-model-spec.md`, chunk index, junk-defaults, and scratch notes.

Recreate the spec from **Step 2** (`parts/steps/modules-epics.md` and following steps) using those chunks only.
