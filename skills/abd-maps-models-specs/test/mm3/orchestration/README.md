# Orchestration output (generated)

This directory is filled by `scripts/orchestrator_loop.py` (`plans/`, `runner/`, `critic/`, `state.json`, `run_summary.json`). Contents are gitignored — run the orchestrator locally or in CI to reproduce.

See `docs/orchestrator.md`.
