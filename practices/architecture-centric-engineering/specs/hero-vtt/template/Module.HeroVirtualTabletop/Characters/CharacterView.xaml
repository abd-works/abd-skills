<UserControl x:Class="Module.HeroVirtualTabletop.Characters.CharacterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <StackPanel>
        <TextBlock Text="{Binding CharacterName}" FontSize="16" Margin="4" />
        <Image Source="/icons/spawned.png"
               Visibility="{Binding IsSpawned, Converter={StaticResource BoolToVis}}"
               Width="16" Height="16" />
        <Button Content="Spawn" Command="{Binding SpawnCommand}" Margin="4" />
        <Button Content="Turn" Command="{Binding TurnCommand}" Margin="4" />
    </StackPanel>
</UserControl>
