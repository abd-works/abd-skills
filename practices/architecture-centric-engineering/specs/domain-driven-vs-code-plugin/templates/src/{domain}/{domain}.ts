// src/{{domain}}/{{domain}}.ts
// Domain entity — pure TypeScript; no vscode, fs, or DOM imports.

export interface I{{Domain}} {
  // TODO: define the domain's command methods and state properties
  // count(amount: number | string): void;
  // reset(): void;
  // readonly total: number;
}

export class {{Domain}} implements I{{Domain}} {
  // TODO: implement the domain entity
  // private _total = 0;

  // count(amount: number | string): void {
  //   this._total += Number(amount);
  // }

  // reset(): void {
  //   this._total = 0;
  // }

  // get total(): number {
  //   return this._total;
  // }
}
