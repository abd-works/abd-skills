<mxfile host="abd-skills" modified="2026-06-24" type="device">
  <diagram id="domain-driven-vscode-plugin" name="Domain-Driven VS Code Plugin">
    <mxGraphModel dx="1422" dy="762" grid="1" gridSize="10" guides="1" tooltips="1" connect="1" arrows="1" fold="1" page="1" pageScale="1" pageWidth="1169" pageHeight="827" math="0" shadow="0">
      <root>
        <mxCell id="0" />
        <mxCell id="1" parent="0" />

        <!-- Title -->
        <mxCell id="title" value="Domain-Driven VS Code Plugin — Architecture Flow" style="text;html=1;strokeColor=none;fillColor=none;align=left;verticalAlign=middle;spacingLeft=4;fontSize=16;fontStyle=1;" vertex="1" parent="1">
          <mxGeometry x="40" y="20" width="600" height="30" as="geometry" />
        </mxCell>

        <!-- ── LAYERS ─────────────────────────────────────────────────────── -->

        <!-- Extension Host layer -->
        <mxCell id="host-bg" value="Extension Host (Node.js)" style="swimlane;startSize=25;fillColor=#dae8fc;strokeColor=#6c8ebf;fontStyle=1;fontSize=11;" vertex="1" parent="1">
          <mxGeometry x="40" y="70" width="500" height="380" as="geometry" />
        </mxCell>

        <!-- Webview layer -->
        <mxCell id="client-bg" value="Webview (Browser / DOM)" style="swimlane;startSize=25;fillColor=#d5e8d4;strokeColor=#82b366;fontStyle=1;fontSize=11;" vertex="1" parent="1">
          <mxGeometry x="600" y="70" width="320" height="380" as="geometry" />
        </mxCell>

        <!-- ── EXTENSION HOST PARTICIPANTS ───────────────────────────────── -->

        <!-- extension.ts -->
        <mxCell id="ext" value="extension.ts&#xa;(composition root)" style="rounded=1;whiteSpace=wrap;html=1;fillColor=#dae8fc;strokeColor=#6c8ebf;fontStyle=1;" vertex="1" parent="host-bg">
          <mxGeometry x="30" y="40" width="160" height="50" as="geometry" />
        </mxCell>

        <!-- EngineView -->
        <mxCell id="engine-view" value="EngineView&#xa;extends BaseView" style="rounded=1;whiteSpace=wrap;html=1;fillColor=#fff2cc;strokeColor=#d6b656;" vertex="1" parent="host-bg">
          <mxGeometry x="30" y="130" width="160" height="50" as="geometry" />
        </mxCell>

        <!-- CounterView -->
        <mxCell id="counter-view" value="CounterView&#xa;extends BaseView, implements ICounter" style="rounded=1;whiteSpace=wrap;html=1;fillColor=#fff2cc;strokeColor=#d6b656;" vertex="1" parent="host-bg">
          <mxGeometry x="30" y="220" width="180" height="50" as="geometry" />
        </mxCell>

        <!-- CounterServer -->
        <mxCell id="counter-server" value="CounterServer&#xa;extends Counter" style="rounded=1;whiteSpace=wrap;html=1;fillColor=#e1d5e7;strokeColor=#9673a6;" vertex="1" parent="host-bg">
          <mxGeometry x="30" y="310" width="160" height="50" as="geometry" />
        </mxCell>

        <!-- Counter (Domain) -->
        <mxCell id="counter-domain" value="Counter&#xa;(pure TS domain)" style="rounded=1;whiteSpace=wrap;html=1;fillColor=#f8cecc;strokeColor=#b85450;" vertex="1" parent="host-bg">
          <mxGeometry x="250" y="310" width="160" height="50" as="geometry" />
        </mxCell>

        <!-- BaseView -->
        <mxCell id="base-view" value="BaseView&#xa;renderTemplate / escapeHtml" style="rounded=1;whiteSpace=wrap;html=1;fillColor=#fff2cc;strokeColor=#d6b656;" vertex="1" parent="host-bg">
          <mxGeometry x="250" y="130" width="180" height="50" as="geometry" />
        </mxCell>

        <!-- ── WEBVIEW PARTICIPANTS ──────────────────────────────────────── -->

        <!-- engine_client.ts -->
        <mxCell id="engine-client" value="engine_client.ts&#xa;(webview entry)" style="rounded=1;whiteSpace=wrap;html=1;fillColor=#d5e8d4;strokeColor=#82b366;fontStyle=1;" vertex="1" parent="client-bg">
          <mxGeometry x="60" y="40" width="180" height="50" as="geometry" />
        </mxCell>

        <!-- domCounter -->
        <mxCell id="dom-counter" value="domCounter&#xa;implements ICounter (DOM)" style="rounded=1;whiteSpace=wrap;html=1;fillColor=#d5e8d4;strokeColor=#82b366;" vertex="1" parent="client-bg">
          <mxGeometry x="60" y="130" width="180" height="50" as="geometry" />
        </mxCell>

        <!-- Counter (bundled) -->
        <mxCell id="counter-bundled" value="Counter&#xa;(bundled — same class)" style="rounded=1;whiteSpace=wrap;html=1;fillColor=#f8cecc;strokeColor=#b85450;fontStyle=2;" vertex="1" parent="client-bg">
          <mxGeometry x="60" y="220" width="180" height="50" as="geometry" />
        </mxCell>

        <!-- HTML / DOM -->
        <mxCell id="dom" value="DOM / HTML&#xa;(Counter.html)" style="rounded=1;whiteSpace=wrap;html=1;fillColor=#d5e8d4;strokeColor=#82b366;" vertex="1" parent="client-bg">
          <mxGeometry x="60" y="310" width="180" height="50" as="geometry" />
        </mxCell>

        <!-- ── EDGES: EXTENSION HOST ─────────────────────────────────────── -->

        <!-- ext → EngineView.createOrShow -->
        <mxCell id="e1" value="createOrShow()" style="edgeStyle=orthogonalEdgeStyle;" edge="1" source="ext" target="engine-view" parent="host-bg">
          <mxGeometry relative="1" as="geometry" />
        </mxCell>

        <!-- EngineView → CounterView._lookup -->
        <mxCell id="e2" value="_lookup('count')" style="edgeStyle=orthogonalEdgeStyle;" edge="1" source="engine-view" target="counter-view" parent="host-bg">
          <mxGeometry relative="1" as="geometry" />
        </mxCell>

        <!-- CounterView → CounterServer -->
        <mxCell id="e3" value="count() / reset()" style="edgeStyle=orthogonalEdgeStyle;" edge="1" source="counter-view" target="counter-server" parent="host-bg">
          <mxGeometry relative="1" as="geometry" />
        </mxCell>

        <!-- CounterServer extends Counter -->
        <mxCell id="e4" value="extends" style="edgeStyle=orthogonalEdgeStyle;endArrow=block;endFill=0;dashed=1;" edge="1" source="counter-server" target="counter-domain" parent="host-bg">
          <mxGeometry relative="1" as="geometry" />
        </mxCell>

        <!-- EngineView / CounterView extend BaseView -->
        <mxCell id="e5" value="extends" style="edgeStyle=orthogonalEdgeStyle;endArrow=block;endFill=0;dashed=1;" edge="1" source="engine-view" target="base-view" parent="host-bg">
          <mxGeometry relative="1" as="geometry" />
        </mxCell>
        <mxCell id="e6" value="extends" style="edgeStyle=orthogonalEdgeStyle;endArrow=block;endFill=0;dashed=1;" edge="1" source="counter-view" target="base-view" parent="host-bg">
          <mxGeometry relative="1" as="geometry" />
        </mxCell>

        <!-- ── EDGES: WEBVIEW ────────────────────────────────────────────── -->

        <!-- engine_client → domCounter -->
        <mxCell id="e7" value="initCounterClient()" style="edgeStyle=orthogonalEdgeStyle;" edge="1" source="engine-client" target="dom-counter" parent="client-bg">
          <mxGeometry relative="1" as="geometry" />
        </mxCell>

        <!-- domCounter uses Counter (bundled) -->
        <mxCell id="e8" value="new Counter()" style="edgeStyle=orthogonalEdgeStyle;" edge="1" source="dom-counter" target="counter-bundled" parent="client-bg">
          <mxGeometry relative="1" as="geometry" />
        </mxCell>

        <!-- domCounter → DOM -->
        <mxCell id="e9" value="update DOM" style="edgeStyle=orthogonalEdgeStyle;" edge="1" source="dom-counter" target="dom" parent="client-bg">
          <mxGeometry relative="1" as="geometry" />
        </mxCell>

        <!-- ── postMessage IPC (crosses swimlane boundary) ──────────────── -->

        <!-- Client → Server postMessage -->
        <mxCell id="ipc1" value="vscode.postMessage&#xa;{ command, args }" style="edgeStyle=orthogonalEdgeStyle;exitX=0;exitY=0.5;exitDx=0;exitDy=0;entryX=1;entryY=0.5;entryDx=0;entryDy=0;strokeColor=#FF6600;fontColor=#FF6600;fontStyle=1;" edge="1" source="dom-counter" target="engine-view" parent="1">
          <mxGeometry relative="1" as="geometry">
            <Array as="points">
              <mxPoint x="580" y="310" />
              <mxPoint x="580" y="255" />
            </Array>
          </mxGeometry>
        </mxCell>

        <!-- Server → Client postMessage -->
        <mxCell id="ipc2" value="panel.webview.postMessage&#xa;{ total }" style="edgeStyle=orthogonalEdgeStyle;exitX=1;exitY=0.5;exitDx=0;exitDy=0;entryX=0;entryY=0.5;entryDx=0;entryDy=0;strokeColor=#009900;fontColor=#009900;fontStyle=1;" edge="1" source="counter-view" target="dom-counter" parent="1">
          <mxGeometry relative="1" as="geometry">
            <Array as="points">
              <mxPoint x="570" y="285" />
              <mxPoint x="570" y="310" />
            </Array>
          </mxGeometry>
        </mxCell>

        <!-- ── LEGEND ─────────────────────────────────────────────────────── -->
        <mxCell id="legend-bg" value="Legend" style="swimlane;startSize=20;fillColor=#f5f5f5;strokeColor=#666666;fontColor=#333333;" vertex="1" parent="1">
          <mxGeometry x="40" y="470" width="300" height="120" as="geometry" />
        </mxCell>

        <mxCell id="leg1" value="Domain entity (pure TS)" style="rounded=1;fillColor=#f8cecc;strokeColor=#b85450;fontSize=10;" vertex="1" parent="legend-bg">
          <mxGeometry x="10" y="30" width="130" height="30" as="geometry" />
        </mxCell>
        <mxCell id="leg2" value="Server view" style="rounded=1;fillColor=#fff2cc;strokeColor=#d6b656;fontSize=10;" vertex="1" parent="legend-bg">
          <mxGeometry x="155" y="30" width="130" height="30" as="geometry" />
        </mxCell>
        <mxCell id="leg3" value="Client (webview)" style="rounded=1;fillColor=#d5e8d4;strokeColor=#82b366;fontSize=10;" vertex="1" parent="legend-bg">
          <mxGeometry x="10" y="75" width="130" height="30" as="geometry" />
        </mxCell>
        <mxCell id="leg4" value="Server domain (persistence)" style="rounded=1;fillColor=#e1d5e7;strokeColor=#9673a6;fontSize=10;" vertex="1" parent="legend-bg">
          <mxGeometry x="155" y="75" width="130" height="30" as="geometry" />
        </mxCell>

      </root>
    </mxGraphModel>
  </diagram>
</mxfile>
