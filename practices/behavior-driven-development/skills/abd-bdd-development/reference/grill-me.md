# Grill me — abd-bdd-development

Only when the invocation includes **"grill me"**. Read [`common/reference/grill-me-with-practice-skill.md`](../../../../common/reference/grill-me-with-practice-skill.md) before grilling.

- How many `it` blocks still contain `// BDD: SIGNATURE` — and what is the implementation order?
- Which shared setup belongs in `beforeEach` or a factory — vs duplicated arrangement?
- Which assertions are observable through the public API — vs private fields or internal spies?
- Where are mocks placed — architecture boundaries only?
- What production code exists without a failing test behind it?
