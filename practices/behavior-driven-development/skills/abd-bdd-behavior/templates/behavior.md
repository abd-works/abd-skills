# HIERARCHY: Feature Name
<!-- Top-level describes = sub-epics from story-map.md -->
<!-- Nested describes = subjects, state elaborations, or narrative events (not class names, not method names) -->
<!-- Leaves = observations of what is true after all containing events have been applied -->

Sub-Epic Name

  a Subject Noun Phrase
    it should observe the initial state
    with a specific state elaboration
      it should observe the narrowed state
      a narrative event that changes the state
        it should observe what is true after the event
        the X should observe another thing true after the event
      an alternative event on the same state
        it should observe what is true after this event instead
    with a different state elaboration
      it should observe this alternative starting state
      an event applied to this state
        it should observe the result

Another Sub-Epic Name

  another Subject
    initial observations
    a state elaboration
      an event
        observations of the result
