# Grill me — abd-bdd-specification

Only when the invocation includes **"grill me"**. Read [`common/reference/grill-me-with-practice-skill.md`](../../../../common/reference/grill-me-with-practice-skill.md) before grilling.

- Is the behavior scaffold approved — and which describe blocks still have open questions?
- Which framework is confirmed — Jest/TypeScript or Mamba/Python?
- Does the declared describe/it nesting match the scaffold 1:1 at every depth?
- Will signatures merge into an existing test file or create a new one?
- Does every `it` body contain only the signature marker — no assertions, mocks, or production imports?
