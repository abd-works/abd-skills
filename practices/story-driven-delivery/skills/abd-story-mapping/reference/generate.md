# Generate — abd-story-mapping

## Fidelity branching

Respect the depth level the user asks for (see `reference/concepts.md` **Depth levels**).

- **Story Map Outline** — epics with confirming stories only; do not apply the full rule set.
- **Level 2+** (default when unspecified) — full hierarchy and all rules.

Follow every file in `rules/`; fill `templates/story-map.md` to match its headings, tables, and fields exactly.
