# Diagram workflow — abd-story-mapping

Shared commands: [`../../../reference/diagram-workflow.md`](../../../reference/diagram-workflow.md)

**This skill:** `--mode outline` → `docs/stories/story-map.drawio`

Run once after `story-graph.json` exists. To sync diagram edits back to the graph, use `sync` per the practice reference (outline diagram).
