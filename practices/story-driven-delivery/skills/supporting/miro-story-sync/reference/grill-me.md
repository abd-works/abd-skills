# Grill me — miro-story-sync

Only when the invocation includes **"grill me"**.

- Which transport will you use — REST with token, or `--dry-run` in-memory?
- Which `--mode` matches the workflow — outline, exploration, or prioritization?
- What board ID and graph path are you rendering from?
- How will you validate the graph with **story-graph-ops** before writing?
- What shared code from `diagram_story_sync` does this skill reuse — and what stays Miro-specific?
