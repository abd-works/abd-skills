import { Stage as DomainStage } from '@deliveryforge/kanban-shared';

/**
 * Presentation-layer Stage — extends domain Stage.
 * Add presentation-specific stage logic (CSS classes, display formatting) here.
 */
export class Stage extends DomainStage {}
