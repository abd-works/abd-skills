import { SkillCatalog as DomainSkillCatalog } from '@deliveryforge/kanban-shared';

/**
 * Presentation-layer SkillCatalog — extends domain SkillCatalog.
 * Add presentation-specific catalog logic (icon mapping, display labels) here.
 */
export class SkillCatalog extends DomainSkillCatalog {}
