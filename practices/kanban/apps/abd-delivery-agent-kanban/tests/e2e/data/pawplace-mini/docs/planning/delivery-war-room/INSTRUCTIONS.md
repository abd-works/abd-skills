# War room — PawPlace mini E2E

**Workspace:** `tests/e2e/data/pawplace-mini`

**Read first:** kanban practice `agents/reference/session-bootstrap.md`, `pull-model.md`, `reference/artifact-layout.md` (deploy skills or read from `practices/kanban/`).

## Scope

Two increments only — see `docs/end-to-end/discovery/stories/thin-slicing.md` and `sprint-groupings.md`.

## War room paths

| File | Purpose |
| --- | --- |
| `board.json` | Tickets, skill_progress |
| `kanban.json` | Stages, stage work required, team |
| `metrics-log.jsonl` | Events |
| `heartbeat-*.json` | Liveness |

Initial ticket: `project-all` in **shaping** (scope: all).
