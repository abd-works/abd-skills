# Output — abd-domain-language

**Deliverables folder:** `<active_skill_workspace>/domain/` (resolve via [`common/reference/skill-workflow.md`](../../../../common/reference/skill-workflow.md) § Output file resolution when the engagement differs).

**File name:** `domain-language.md`. Add a `<name>-` prefix only when disambiguation is needed.

**Multi-module:** `<deliverables-folder>/modules/<module-name>-domain-language.md`. When the scope spans more than one module in a single file, organize with `# Module: [ModuleName]` sections.
