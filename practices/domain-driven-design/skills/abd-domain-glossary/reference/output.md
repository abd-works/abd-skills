# Output — abd-domain-glossary

**Deliverables folder:** `<active_skill_workspace>/domain/` (resolve via [`common/reference/skill-workflow.md`](../../../../common/reference/skill-workflow.md) § Output file resolution when the engagement differs).

**Single file** (default): `domain/domain-glossary.md` — all modules, KAs, and terms in one file.

**Per-module files** (large systems only): `domain/domain-glossary/<module-name>.md` — one file per module when a single file would be unnavigable.
