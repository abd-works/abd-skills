# Requirements

Optional layered requirement files (e.g. `base.txt`, `dev.txt`) if you standardize on `pip install -r` in CI.

Primary source of truth remains **`pyproject.toml`** at the repository root.
