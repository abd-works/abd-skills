# Configuration

Place **non-secret** defaults here (graph names, paths templates, feature flags).

Secrets (API keys, tokens) stay in environment variables or a gitignored `conf/local.secrets.example` pattern — do not commit real secrets.
