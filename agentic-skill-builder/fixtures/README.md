# Fixtures

The minimal valid skill example (**toy polite dialogue**) is maintained under **`skills/abd-skill-builder/test/fixture/toy-polite-dialogue/`** at the repo root.

This package’s tests resolve that path via the monorepo layout (`Path(…)/ parents[2] / "skills" / "abd-skill-builder" / …`).
