# Open Agent Skill

Created from **abd-skill-builder** **`skills/build_skill/templates/skill-scaffold/`**. Long-form authoring process, layout conventions, and phase behavior are maintained in the **abd-skill-builder** repo—this file only orients you; it does not duplicate them.

**Work here:** `content/parts/process.md`, `content/parts/phases/*.md`, and `skill-config.json`. Shared norms (including **`library/base/checklist.md`**) are copied with **`library/base/`** at scaffold.

**Canonical reference (abd-skill-builder checkout):** `content/parts/library/skill-structure-and-concepts.md`, `content/outline.md`.

Assemble **`AGENTS.md`** with `python scripts/base/build.py`.
