# `content/parts/phases/built/` — derived per-phase prompts

When **`build.py`** is configured to emit static phase slices, snapshots land here. Do not hand-edit; regenerate from `content/parts/phases/` and `scripts/base/build.py`.
