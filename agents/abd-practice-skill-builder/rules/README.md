# Rules for abd-practice-skill-builder

Optional. Add `*.md` rule files here if you want them inlined into `AGENTS.md`. Otherwise normative text lives only in **`AGENTS.md`**; quality and corrections are defined in **`skills/execute_using_rules/SKILL.md`**.

Run `python scripts/build.py` from the agent root after adding or changing rule files.
