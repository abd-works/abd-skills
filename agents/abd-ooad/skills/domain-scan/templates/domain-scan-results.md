# Domain Scan Results — {{project_name}}

## Source Map

<!--
  Column-width tip: most markdown previews (Cursor, GitHub, VS Code) auto-size
  columns by content and ignore inline `style` / `width` attributes. Keep
  Section labels short (move long parenthetical examples into Notes), and
  abbreviate the Signal column to a single character (H / M / L) so the middle
  column stays narrow and Notes naturally fills the remaining width.
-->

**Signal:** H = High · M = Medium · L = Low.

| Section / Module | Sig | Notes |
|---|:---:|---|
| {{section}} | H / M / L | {{what_looks_heavy_or_relevant}} |

---
